---
description: Generate or refine a design system (tokens, components, patterns)
argument-hint: [project brief or component to systemize]
---

Load and apply the `design-system` skill from `.claude/skills/design-system/SKILL.md`.

Read the skill file now, then handle the following request:

$ARGUMENTS
