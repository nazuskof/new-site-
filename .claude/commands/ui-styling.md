---
description: Apply UI styling (glassmorphism, brutalism, neumorphism, etc.)
argument-hint: [component or screen + desired style]
---

Load and apply the `ui-styling` skill from `.claude/skills/ui-styling/SKILL.md`.

Read the skill file now, then handle the following request:

$ARGUMENTS
