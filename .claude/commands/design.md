---
description: Apply the `design` skill for general UI/UX design tasks
argument-hint: [what to design]
---

Load and apply the `design` skill from `.claude/skills/design/SKILL.md`.

Read the skill file now, then handle the following request:

$ARGUMENTS
